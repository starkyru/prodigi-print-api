# CLAUDE.md

## Conventions

- When adding or modifying public methods/classes, always update JSDoc comments to stay in sync with the code.
- When adding, removing, or changing API methods in `src/resources/`, update the explorer app's method registry (`explorer/src/method-registry.ts`) to keep it in sync.
